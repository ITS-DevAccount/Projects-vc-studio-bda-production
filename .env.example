# Supabase Configuration
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anonymous/public API key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# MULTI-APP CONFIGURATION (Phase 1a)
# =============================================================================

# Which application is this instance running as?
# This determines which app's data is shown and which branding is used
#
# Available options:
#   - VC_STUDIO (Build Domain Architecture - BDA)
#   - T2G        (Advise Domain Architecture - ADA)
#   - G2G        (Advise Domain Architecture - ADA)
#   - OCG        (Advise Domain Architecture - ADA)
#   - BUILDBID   (Plan Domain Architecture - PDA)
#
# Default: VC_STUDIO
NEXT_PUBLIC_SITE_CODE=VC_STUDIO

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Fill in your actual Supabase credentials in .env.local
#
# 3. For Vercel deployment:
#    - Go to Project Settings → Environment Variables
#    - Add both variables above
#    - Make sure to check all environments (Production, Preview, Development)
#    - IMPORTANT: Copy values carefully - no extra spaces or line breaks!
#
# =============================================================================
# IMPORTANT NOTES
# =============================================================================
#
# - Never commit .env.local to git (it's in .gitignore)
# - Always use NEXT_PUBLIC_ prefix for client-side variables
# - Vercel will automatically use these environment variables
# - You can import .env file in Vercel dashboard (Settings → Environment Variables → Import)
#
# =============================================================================
# TROUBLESHOOTING
# =============================================================================
#
# If you see "Failed to execute 'fetch'" error:
# - Check that variable names are EXACTLY as shown above
# - Ensure no extra spaces before/after values
# - Ensure no carriage returns (CR) or line breaks in the values
# - Check that variables are set for the correct environment
#
