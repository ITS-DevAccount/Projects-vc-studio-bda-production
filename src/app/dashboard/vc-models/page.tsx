'use client';

import VCModelsList from '@/components/vc-models/VCModelsList';

export default function VCModelsPage() {
  return (
    <div className="container mx-auto py-8 px-4">
      <VCModelsList />
    </div>
  );
}
